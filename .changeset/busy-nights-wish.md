---
'@reuters-graphics/graphics-kit': patch
---

Adheres to new ISRM rules: Configures Git to use main as the default global branch, pushes the initial project setup to the main branch, and creates a new branch gfx with upstream tracking set to the remote repository
