---
'@reuters-graphics/graphics-kit': patch
---

Bumps @reuters-graphics/graphics-components to ^4.0.0. Its only breaking change (a `base` string prop replaced with a `resolve` function prop on `LanguageButton`, `BlogTOC`, and `BlogPost`) doesn't affect this template, which doesn't use those components.
