---
'@reuters-graphics/graphics-kit': patch
---

Bumps svelte, @sveltejs/kit, vite, and vitest to their latest versions, and finishes migrating the remaining `assets`/`base` usages (SEO canonical URLs, embed-previewer's back link) to `asset(...)`/`resolve(...)`.
