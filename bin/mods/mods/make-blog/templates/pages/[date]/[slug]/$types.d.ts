// Stub for SvelteKit's generated `./$types` so the template type-checks in this
// repo. Not copied into projects (the real types are generated by svelte-kit).
export type PageData = any;
export type PageServerLoad = any;
